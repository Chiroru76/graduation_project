# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

green_robo_stage1_idle:
  character_kind: one   # character_kinds.yml の one を参照
  pose: idle
  asset_kind: webp

green_robo_stage2_idle:
  character_kind: two   # character_kinds.yml の two を参照
  pose: idle
  asset_kind: webp